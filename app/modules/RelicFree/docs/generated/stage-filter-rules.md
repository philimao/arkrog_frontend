<!-- AUTO-GENERATED by scripts/docs-gen.mjs — 勿手改，运行 pnpm docs:gen 重新生成 -->
---
generated: true
sources:
  - app/utils/stageSelector.ts
---

# 无藏关卡筛选规则表（navOfZone）

本清单由 `scripts/docs-gen.mjs` 从 `app/utils/stageSelector.ts` 的 `navOfZone` 数组提取，按源码出现顺序排列（即页面导航顺序）。filter 判定的输入是 stage id 按 `_` 分段后的各段，id 语法与分段语义见 [03-stage-taxonomy-and-selector.md](../03-stage-taxonomy-and-selector.md)。

> ⚠️ `boss` 组的 filter 是**双参签名** `(stage, array)`，并对传入数组执行 `array.push(stage.name)` **副作用**做按名去重——调用方必须在一次遍历中对该组复用同一数组；其余组均为单参纯函数。给 boss 组每次调用传新数组会使去重失效。

## 总览

| id | 名称 | 语义 |
|---|---|---|
| `boss` | 险路恶敌 | 大 Boss 关：仅收作战段为 b 且编号大于该主题 numOfMinorBoss（前三层小 Boss 关计数）的关卡，剔除 excludeIds，并借调用方传入的数组按 stage.name 去重（同名只收一次） |
| `6` | 第六层 | 普通作战（作战段 n）第六层；源码注释注明洞天福地是 7 层，其第 7 层归入本组 |
| `5` | 第五层 | 普通作战（作战段 n）第五层 |
| `4` | 第四层 | 普通作战（作战段 n）第四层 |
| `zone_sky_1` | 是非境 | 是非境：作战段为 sv 且 id 末段不是 dlc1 |
| `zone_sky_2` | 今昔境 | 今昔境：作战段为 sv 且 id 末段是 dlc1（DLC1 新增分境） |
| `zone_portal` | 未萌生的摇篮 | 未萌生的摇篮：作战段为 c（黑流树海 ro6 新增的 GRID_ZONE 传送门关卡类型码） |
| `others` | 特殊关卡 | 特殊关卡：作战段为 ev/t（不期而遇）、duel（狭路）、dv（分明） |

## 各筛选器源码摘录

### 险路恶敌（`boss`）

大 Boss 关：仅收作战段为 b 且编号大于该主题 numOfMinorBoss（前三层小 Boss 关计数）的关卡，剔除 excludeIds，并借调用方传入的数组按 stage.name 去重（同名只收一次）。

```ts
(stage: StageData, array: string[]) => {
  const excludeIds = ["ro4_b_9"];
  if (excludeIds.includes(stage.id)) return false;
  const args = stage.id.split("_");
  // eg: ro4_b_5_d
  if (args[1] !== "b") return false;
  // if (args[3]) return false; // 异格
  const cool =
    parseInt(args[2]) > // 如果该boss编号是小boss，跳过
    (numOfMinorBoss[args[0] as keyof typeof numOfMinorBoss] || 99);
  if (!cool || array.includes(stage.name)) return false;
  array.push(stage.name);
  return true;
}
```

### 第六层（`6`）

普通作战（作战段 n）第六层；源码注释注明洞天福地是 7 层，其第 7 层归入本组。

```ts
(stage: StageData) => {
  const args = stage.id.split("_");
  if (args[1] !== "n") return false;
  return args[2] === "6" || args[2] === "7"; // 洞天福地是7层
}
```

### 第五层（`5`）

普通作战（作战段 n）第五层。

```ts
(stage: StageData) => {
  const args = stage.id.split("_");
  if (args[1] !== "n") return false;
  return args[2] === "5";
}
```

### 第四层（`4`）

普通作战（作战段 n）第四层。

```ts
(stage: StageData) => {
  const args = stage.id.split("_");
  if (args[1] !== "n") return false;
  return args[2] === "4";
}
```

### 是非境（`zone_sky_1`）

是非境：作战段为 sv 且 id 末段不是 dlc1。

```ts
(stage: StageData) => {
  const args = stage.id.split("_");
  if (args[1] !== "sv") return false;
  return args.slice(-1)[0] !== "dlc1";
}
```

### 今昔境（`zone_sky_2`）

今昔境：作战段为 sv 且 id 末段是 dlc1（DLC1 新增分境）。

```ts
(stage: StageData) => {
  const args = stage.id.split("_");
  if (args[1] !== "sv") return false;
  return args.slice(-1)[0] === "dlc1";
}
```

### 未萌生的摇篮（`zone_portal`）

未萌生的摇篮：作战段为 c（黑流树海 ro6 新增的 GRID_ZONE 传送门关卡类型码）。

```ts
(stage: StageData) => {
  const args = stage.id.split("_");
  return args[1] === "c"; // 黑流树海 GRID_ZONE 传送门关卡
}
```

### 特殊关卡（`others`）

特殊关卡：作战段为 ev/t（不期而遇）、duel（狭路）、dv（分明）。

```ts
(stage: StageData) => {
  const args = stage.id.split("_");
  return [
    "ev", // 不期而遇
    "t", // 不期而遇
    "duel", // 狭路
    "dv", //分明
  ].includes(args[1]);
}
```

## numOfMinorBoss 快照

各主题前三层小 Boss 关计数（源码注释：三层boss关数量，异格记为同一个）。`boss` 组只收编号**大于**该值的关卡。**版本敏感**：新主题上线必须补行，且与后端同值表保持一致，见 [version-sensitive-hardcode.md](../version-sensitive-hardcode.md) 与 [new-topic-checklist.md](../new-topic-checklist.md)。

| 主题 | 小 Boss 编号上限 | 源码行内注释 |
|---|---|---|
| `ro1` | 5 | — |
| `ro2` | 3 | — |
| `ro3` | 3 | — |
| `ro4` | 3 | — |
| `ro5` | 3 | — |
| `ro6` | 3 | 黑流树海：b_1~b_3 为三层小boss，不计入险路恶敌（与后端 numOfZone3Boss 同值） |

共 6 个主题。缺项时 filter 以 99 兜底（新主题大 Boss 关全部不显示，静默失败）。

## excludeIds 快照

`boss` 组显式剔除的关卡 id（硬编码于 filter 函数体内）。

| 关卡 id | 源码行内注释 |
|---|---|
| `ro4_b_9` | — |

共 1 项。
